[package]
name = "infers"
version = "0.1.0"
edition = "2024"

[dependencies]
cudarc = { version = "0.18.1", features = ["cuda-version-from-build-system"] }
num-traits = "0.2.19"
rand = "0.9.2"

[features]
default = ["cuda"]
cuda = []
